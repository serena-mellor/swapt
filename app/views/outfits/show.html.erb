<div class="container">
  <%= render "shared/hamburger_menu" %>
    <h2 class="outfit-heading"><%= @outfit.name.upcase %></h2>
    <div class="outfit-container">
      <% @outfit.sort_items_by_position.each do |item| %>
        <%= cl_image_tag item.photo.key, height: 200, width: 200 %>
      <% end %>
    </div>
</div>
